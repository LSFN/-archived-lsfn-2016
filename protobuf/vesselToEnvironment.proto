syntax = "proto3";
package protobuf;

message JoinRequest {
	string shipServerID = 1; // If reconnecting, the ID of this ship server previously issued by the environment.
}

message vesselToEnvironment {
	string protocolVersion = 1;
	JoinRequest joinRequest = 2;
}